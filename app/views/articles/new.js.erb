var el = document.getElementById('<%= new_article_modal_id %>');
el.outerHTML = '<%= j render(partial: 'form', locals: { event: @event, article: @article }) %>';

// Stimulus controller handles the rest.
